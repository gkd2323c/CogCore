# CogCore 设计文档

> 这是 CogCore 项目的"怎么做"——回答"高层架构、关键决策、设计原则"。详细模块设计、接口契约、配置参数见 `docs/CogCore-通用认知内核架构设计.md`。

---

## 1. 高层架构

CogCore 处于 LLM Agent 系统的认知层，与 LLM 解释层、工具执行层、教师审查层形成四层分工：

```
┌────────────────────────────────────────────┐
│  上层 Agent 系统                              │
│  (LLM 解释 / 对话界面 / 工具链 / 安全审查)   │
└──────────┬──────────────────────┬──────────┘
           │ 外源输入               │ 行动结果 / 教师反馈
           ▼                      ▲
┌────────────────────────────────────────────┐
│            CogCore 认知内核                  │
│                                            │
│  感受器层  →  状态池  →  HDB 查存一体        │
│     ↓           ↓           ↓              │
│  感应生长  →  注意力  →  CAM 回流            │
│     ↓           ↓           ↓              │
│  CFS 认知感受  →  NT 情绪递质  →  行动节点   │
│                                            │
│         自适应调参器（稳定运行环境）           │
└────────────────────────────────────────────┘
```

## 2. 四大设计原则

| 原则 | 含义 | 工程后果 |
|------|------|---------|
| **白箱优先** | 每个认知对象记录来源、能量、匹配路径 | 不用向量数据库做核心匹配；可审计 |
| **闭环而非流水线** | 行动后果/残差/感受回写状态池 | 状态池是核心数据结构，所有模块读写它 |
| **能量即货币** | 实能量（现实证据）+ 虚能量（内源预测）+ 认知压（张力） | 统一度量重要性、竞争、衰减、调制 |
| **可衰减/可竞争/可召回** | 状态池是动态场而非数据库 | 模块必须考虑生命周期、能量预算 |

## 3. 关键决策

### 3.1 底层框架：LangGraph

| 选项 | 评估 | 决策 |
|------|------|------|
| LangGraph | StateGraph 天然映射 10 阶段 tick；Reducer + Checkpoint + Store 三层架构 | **首选** |
| PydanticAI + Temporal | 类型安全但成熟度不足 | 次选 |
| OpenAI Agents SDK | 模型锁定、架构过简 | 不选 |
| Smolagents / CrewAI / AG2 | 缺状态管理或不适合管线模型 | 不选 |

详见 `docs/cogcore_framework_research.md`。

### 3.2 记忆机制：HDB（查存一体），不用向量库

HDB 做的是**局部索引 + 残差写入**，与向量数据库的全局相似性检索根本不同：

- 向量库："从全部历史中找最像的"——黑箱、不可解释
- HDB："从当前命中的局部经验中看下一步可能是什么"——可解释路径

代价：字符级处理的语义泛化效率较低。可通过感受器层注册词级/向量 parser 弥补。

### 3.3 情绪处理：CFS + NT，不是提示词

- CFS（认知感受）：违和感、正确感、期待、压力——**作为可入池的认知对象**
- NT（情绪递质）：多通道调制信号（focus/arousal/caution/exploration/fatigue/stability）——**作为全局参数调制**

情绪不只改输出风格，而是真参与信息处理：违和感让对象更易被注意，奖励信号让行动阈值下降。

### 3.4 主动性：三种运行模式

| 模式 | 行为 | 适合阶段 |
|------|------|---------|
| `full_silent` | 只在明确触发下响应 | 冷启动、保守验证 |
| `ap_agency` | AP 可基于 wake_drive 主动升起候选 | 测试主观能动性 |
| `reinforced_agency` | 主动性还需经过教师门控 | 长期发展 + 审查纠偏 |

从 `full_silent` 起步，再开 `ap_agency`，最后上 `reinforced_agency`。

## 4. 与论文 AP 的关系

| 项 | AP 原型（论文） | CogCore |
|---|---|---|
| 拓扑 | 单进程 Python + observatory | LangGraph StateGraph（10 节点）|
| 持久化 | 自带 state_pool + HDB | 短期 Checkpointer + 长期 Store |
| 状态合并 | 各模块内部维护 | 统一 Reducer 机制 |
| 工具调用 | LocalToolRegistry | LangGraph ToolNode + tool_allowlist |
| 教师反馈 | 延迟合流 `_queue_teacher_feedback_labels` | 同样延迟合流 `queue_teacher_feedback` |
| 观测台 | observatory/_app.py | 保留 Observatory 接口，底层走 LangSmith |

**接口对齐原则**：保留所有外部可观察接口的语义，内部实现可替换。

## 5. 详细设计入口

| 想了解什么 | 看哪里 |
|----------|------|
| 模块接口、数据结构、配置参数 | `docs/CogCore-通用认知内核架构设计.md` |
| E01-E17 实验验证矩阵 | `docs/CogCore-验证矩阵.md` |
| LangGraph 选型对比 | `docs/cogcore_framework_research.md` |
| 论文 AP 深度分析 | `docs/人工心智架构-论文分析.md` |
| 怎么把 CogCore 组装成完整智能体 | [`AGENT_BUILD.MD`](./AGENT_BUILD.MD)（4 个参考项目能力地图 + 11 层能力栈） |

---

*本文档是项目门面；详细设计、接口契约、参数表见 `docs/`。*
