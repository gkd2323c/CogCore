# E01 历史顺序改变后续同序输入的结构复用成本 — 实验终稿报告

## 一、运行结果汇总

| 指标 | 实测值 | 目标值 | 状态 |
|------|--------|--------|------|
| Case 1 match_score（乱序历史） | 1.0000 | — | — |
| Case 2 match_score（同序历史） | 1.0000 | — | — |
| **存储优势** | **1.0000** | **2.300** | **未通过** |

## 二、分支数据分析

1. **Case 1（同序→乱序→探针）**：
   先以同序 "A B C D E" × 5 建立结构，再以乱序 "E D C B A" × 5 干扰。探针 match_score = 1.0000。结构中存在 1 个节点。

2. **Case 2（乱序→同序→探针）**：
   先以乱序 × 5，再以同序 × 5 修正。探针 match_score = 1.0000。

## 三、结论

存储优势未达目标，当前 HDB 的 2-gram tokenization 对同序/乱序的敏感度不足以产生预期分化。需要增强 HDB 的顺序敏感匹配机制（如引入位置编码或 n-gram 跨原子连接）。
