# CogCore 需求文档

> 这是 CogCore 项目的"为什么"——回答"为什么需要它、解决什么问题、不做什么、什么算成功"。

---

## 1. 背景与问题

当前以 LLM 为核心的 Agent 系统在长期运行场景下存在七类结构性困难（论文 AP 第 1.2 节归纳）：

1. **持续学习与长期记忆外置化**——记忆与认知过程割裂
2. **多模态与符号接地缺少统一闭环**——压成文字后结构信息丢失
3. **长上下文与大规模检索带来成本和选择压力**——没有"此刻应该活跃什么"的机制
4. **黑箱推理链难以支持机制级审计**——"为什么想起这件事"不可追踪
5. **情绪和人格停留在表达层**——情绪不参与信息处理
6. **主动性退化为外部调度**——定时任务而非内生驱动力
7. **长期人格连续性缺少底层载体**——人设提示词每轮重新表演

这七类问题**不是孤立的缺陷**，而是同一个长期运行问题在不同位置的表现。

## 2. 目标

CogCore 补 LLM 在长期连续性上的结构性缺失，承担四件事：

- **长期状态维护**——跨 tick 持续的认知场
- **可解释记忆**——白箱追溯每个认知对象从哪来到哪去
- **内源感受调制**——让系统能感知自身处理状态并影响后续
- **行动反馈学习**——把行动后果和奖惩回写为可复用经验

LLM 继续承担它擅长的事：语言生成、工具编排、安全审查。CogCore 在它下面一层，专注持续状态。

## 3. 用户场景

| 场景 | 关键需求 |
|------|---------|
| **长期陪伴 Agent** | 跨会话记忆连续、用户偏好沉淀、行动习惯形成 |
| **研究型 Agent** | 完整白箱可审计、每个决策可回溯到内部状态 |
| **多 Agent 共享认知层** | 跨 Agent 共享结构、情景、行动节点 |

## 4. 不做什么

明确范围边界，避免后续 scope creep：

- **不替代 LLM**——LLM 仍是解释层、工具编排者
- **不解决开放自然语言组织质量**——E17 仅验证候选链雏形，叙事质量评测是下一阶段
- **不做多模态具身控制闭环**——E16 验证属性化入口，不验证机器人实时控制
- **不做超长时程自我模型稳定性**——3000+ tick 测试是 M2 里程碑
- **不做大规模技能包迁移**——技能包协议是 M2 内容
- **不追求所有 Agent 场景**——CogCore 是"认知层"而非"Agent 框架"

> **诚实原则**：上述未做的项目在所有文档中保持"未通过"状态，**不允许**把"计划"标成"已通过"。

## 5. 成功标准

CogCore 的成功由 `docs/CogCore-验证矩阵.md` 中的 E01-E17 强证据定义：

| 阶段 | 成功标准 | 退出条件 |
|------|---------|---------|
| **M0** | 实现 10 阶段 tick 流水线 + 9 个核心模块 | E01-E17 全部通过准入 4 条件 |
| **M1** | LangGraph 集成 + LLM 双层结构 + 长期经验工具 | E13 在真实 Agent 上下文中通过 |
| **M2** | 三种运行模式全部启用 + 技能包协议 + 3000+ tick 稳定 | E18-E25（未来实验）通过 |

**914 条判据记录**（论文总计）是 M0 阶段必须独立复现的硬指标。

## 6. 关系

CogCore 不是从零开始的设想，而是论文《人工心智架构》（Artificial PsyArch, AP）原型的工程化重写。论文 113 页给出了完整的理论闭环、17 项强证据实验、3 阶段发展路径。CogCore 按此路径逐步实现并复现。

- 论文是预言者
- CogCore 是实现者
- 验证矩阵是承诺与证据的桥梁

---

*详细设计、模块接口、配置参数、验证矩阵分别见 `docs/` 下的对应文档。*
